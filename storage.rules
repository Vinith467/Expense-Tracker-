rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Receipts/attachments are namespaced by uid: receipts/{uid}/{expenseId}/{file}
    match /receipts/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
